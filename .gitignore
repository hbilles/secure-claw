node_modules/
dist/
.env
/data/
*.sock
*.tsbuildinfo
.claude

# Vendored package build artifacts (rebuilt inside Docker)
vendor/*/package-lock.json

# Local runtime config (contains environment-specific values and may contain secrets)
config/secureclaw.yaml

# macOS Finder metadata
.DS_Store
**/.DS_Store
